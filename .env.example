# Copy to a real environment variable before running the server.
# Never commit your real key.
OPENAI_API_KEY=sk-your-openai-key-with-realtime-access
# Optional: change the port (default 3000)
# PORT=3000
